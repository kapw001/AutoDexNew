<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="UserCustomView">
        <attr name="text" format="string" />
        <attr name="hint" format="string" />
        <attr name="image" format="reference"></attr>
        <attr name="alpha" format="float"></attr>
        <attr name="inputType" format="string"></attr>
        <attr name="isDateEditable" format="boolean"></attr>
        <attr name="isShow" format="boolean"></attr>
    </declare-styleable>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>
</resources>